# Dockerfile.base
FROM python:3.11-slim

RUN apt-get update && apt-get install -y \
    libgtk-3-dev \
    libboost-all-dev \
    libopenblas-dev \
    liblapack-dev \
    && rm -rf /var/lib/apt/lists/*